<div class="content__post" style="background-image: url(<%= tweet.photo.url %>);">
  <p>
    <%= br(tweet.text) %>
  </p>
  <% if tweet.user_id %>
    <span class="name"><%= tweet.user_id %></span>
  <% end %>
</div>